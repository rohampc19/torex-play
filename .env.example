# TOREX PLAY environment
# Copy to .env locally when you add an env loader or export variables in your shell.

PORT=3000
NODE_ENV=development
SESSION_TTL_MS=86400000
ADMIN_KEY=change-this-before-production
DATA_FILE=./torex-data.json
