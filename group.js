"use strict";
document.addEventListener("DOMContentLoaded", () => { const api = window.PERFASHINALRequest; const root = document.getElementById("groupView"); const id = new URLSearchParams(location.search).get("id"); const esc = window.escapeHtml || (value => String(value)); let group;
async function load() { try { const result = await api(`/groups/${encodeURIComponent(id)}`); group = result.group; render(); } catch (e) { root.innerHTML = `<p class="error-state">${esc(e.message)}</p>`; } }
function render() { const members = group.members.map(member => `<li><span class="member-avatar">${esc(member.avatar)}</span>${esc(member.name || member.username)}${member.role === "leader" ? " (لیدر)" : ""}</li>`).join(""); root.innerHTML = `<article class="group-detail"><header class="group-detail-head"><span class="group-avatar group-avatar-large">${group.avatar ? `<img src="${esc(group.avatar)}" alt="">` : esc(group.name[0])}</span><div><h1>${esc(group.name)}</h1><p>${esc(group.description || "بدون توضیحات")}</p><div class="group-meta"><span>لیدر: ${esc(group.leader?.name || group.leader?.username)}</span><span>اعضا: ${group.memberCount}/${group.maxMembers}</span></div></div></header><div class="group-detail-grid"><section class="group-members"><h2>اعضا</h2><ul>${members || "<li>هنوز عضوی نیست.</li>"}</ul>${group.isMember && !group.isLeader ? `<button class="second-button" id="leaveGroup">خروج از گروه</button>` : ""}${group.isLeader ? `<button class="second-button" id="editGroup">ویرایش اطلاعات</button><button class="second-button" id="groupRequests">درخواست‌های عضویت</button><button class="danger-button" id="deleteGroup">حذف گروه</button>` : ""}</section><section class="group-chat"><h2>چت گروه</h2><div class="group-chat-messages" id="groupMessages"><p class="empty-state">در حال دریافت پیام‌ها...</p></div>${group.isMember ? `<form id="groupMessageForm"><input id="groupMessageInput" maxlength="500" placeholder="پیام خود را بنویسید..." autocomplete="off"><button class="main-button">ارسال</button></form>` : "<p class="muted">فقط اعضای گروه به چت دسترسی دارند.</p>"}</section></div></article>`; bind(); }
async function bind() { document.getElementById("editGroup")?.addEventListener("click", async () => { const description = prompt("توضیحات جدید گروه:", group.description || ""); if (description === null) return; try { await api(`/groups/${id}`, { method: "PATCH", body: JSON.stringify({ description }) }); await load(); } catch (e) { window.WEXORAChatToast?.(e.message, "error"); } }); document.getElementById("groupRequests")?.addEventListener("click", async () => { try { const result = await api(`/groups/${id}/requests`); if (!result.requests.length) return window.WEXORAChatToast?.("درخواست معلقی وجود ندارد."); const request = result.requests[0]; if (!confirm(`درخواست عضویت ${request.user?.username || "کاربر"} قبول شود؟`)) return; await api(`/groups/${id}/requests`, { method: "POST", body: JSON.stringify({ requestId: request.id, action: "approve" }) }); await load(); } catch (e) { window.WEXORAChatToast?.(e.message, "error"); } }); document.getElementById("leaveGroup")?.addEventListener("click", async () => { try { await api(`/groups/${id}/leave`, { method: "POST" }); location.href = "groups.html"; } catch (e) { window.WEXORAChatToast?.(e.message, "error"); } }); document.getElementById("deleteGroup")?.addEventListener("click", async () => { if (!confirm("آیا مطمئن هستید که می‌خواهید این گروه را حذف کنید؟")) return; try { await api(`/groups/${id}`, { method: "DELETE" }); location.href = "groups.html"; } catch (e) { window.WEXORAChatToast?.(e.message, "error"); } }); const form = document.getElementById("groupMessageForm"); if (!form) return; const messages = document.getElementById("groupMessages"); async function loadMessages() { try { const result = await api(`/groups/${id}/messages`); messages.innerHTML = result.messages.length ? result.messages.map(item => `<p><strong>${esc(item.user)}</strong> ${esc(item.message)}</p>`).join("") : `<p class="empty-state">هنوز پیامی نیست.</p>`; messages.scrollTop = messages.scrollHeight; } catch (e) { messages.innerHTML = `<p class="error-state">${esc(e.message)}</p>`; } form.onsubmit = async event => { event.preventDefault(); const input = document.getElementById("groupMessageInput"); if (!input.value.trim()) return; try { await api(`/groups/${id}/messages`, { method: "POST", body: JSON.stringify({ text: input.value.trim() }) }); input.value = ""; loadMessages(); } catch (e) { window.WEXORAChatToast?.(e.message, "error"); } }; loadMessages(); setInterval(loadMessages, 4000); }
load(); });