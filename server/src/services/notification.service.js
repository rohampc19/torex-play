import {pool} from '../config/db.js'; export async function notify(userId,type,title,body){const{rows}=await pool.query('INSERT INTO notifications(user_id,type,title,body) VALUES($1,$2,$3,$4) RETURNING *',[userId,type,title,body]);return rows[0]} export async function listNotifications(userId){const{rows}=await pool.query('SELECT * FROM notifications WHERE user_id=$1 ORDER BY created_at DESC LIMIT 100',[userId]);return rows} export async function markRead(userId,id){const{rows}=await pool.query('UPDATE notifications SET read_at=NOW() WHERE id=$1 AND user_id=$2 RETURNING *',[id,userId]);return rows[0]}