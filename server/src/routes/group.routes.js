import {Router} from 'express'; import {z} from 'zod'; import {requireAuth} from '../middleware/auth.js'; import {validate} from '../middleware/validate.js'; import * as c from '../controllers/group.controller.js'; const create=z.object({name:z.string().trim().min(2).max(80),description:z.string().max(500).optional(),avatarUrl:z.string().url().optional(),visibility:z.enum(['public','private'])}); export const groupRouter=Router();groupRouter.get('/',c.list);groupRouter.post('/',requireAuth,validate(create),c.create);groupRouter.post('/:id/join',requireAuth,c.join);groupRouter.delete('/:id/leave',requireAuth,c.leave);