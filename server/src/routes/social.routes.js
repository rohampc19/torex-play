import {Router} from 'express'; import {z} from 'zod'; import {requireAuth} from '../middleware/auth.js'; import {validate} from '../middleware/validate.js'; import * as c from '../controllers/social.controller.js'; export const socialRouter=Router(); socialRouter.post('/users/:id/follow',requireAuth,c.follow);socialRouter.delete('/users/:id/follow',requireAuth,c.unfollow);socialRouter.post('/users/:id/friends',requireAuth,c.friendRequest);socialRouter.patch('/users/:id/friends',requireAuth,validate(z.object({status:z.enum(['accepted','blocked'])})),c.friendshipAction);