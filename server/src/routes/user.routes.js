import {Router} from 'express'; import {z} from 'zod'; import {requireAuth} from '../middleware/auth.js'; import {validate} from '../middleware/validate.js'; import * as c from '../controllers/user.controller.js'; const update=z.object({displayName:z.string().trim().min(2).max(60).optional(),bio:z.string().max(500).optional(),avatarUrl:z.string().url().max(1000).nullable().optional(),favoriteGames:z.array(z.string().min(2).max(80)).max(10).optional()}); export const userRouter=Router(); userRouter.get('/search',requireAuth,c.search);userRouter.get('/me',requireAuth,c.me);userRouter.get('/:id',requireAuth,c.profile);userRouter.patch('/me',requireAuth,validate(update),c.update);