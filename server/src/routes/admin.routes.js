import {Router} from 'express'; import {z} from 'zod'; import {requireAuth} from '../middleware/auth.js'; import {requireRole} from '../middleware/roles.js'; import {validate} from '../middleware/validate.js'; import * as c from '../controllers/admin.controller.js'; export const adminRouter=Router();adminRouter.use(requireAuth,requireRole('admin'));adminRouter.get('/stats',c.stats);adminRouter.get('/pending-users',c.pending);adminRouter.patch('/users/:id/status',validate(z.object({status:z.enum(['approved','rejected','blocked'])})),c.setStatus);adminRouter.post('/users/:id/block',c.block);