import {Router} from 'express';import {z} from 'zod';import {validate} from '../middleware/validate.js';import {requireAuth} from '../middleware/auth.js';import * as c from '../controllers/report.controller.js';const r=Router();r.post('/',requireAuth,validate(z.object({kind:z.enum(['message','user','news','group']),targetId:z.string().min(1).max(120),reason:z.string().min(3).max(500)})),c.create);export default r;
