import mongoose from 'mongoose';const messageSchema=new mongoose.Schema({conversationKey:{type:String,index:true},groupId:{type:String,index:true},from:{type:String,required:true,index:true},to:{type:String,index:true},text:{type:String,required:true,maxlength:2000},deliveredAt:Date,seenAt:Date},{timestamps:true});messageSchema.index({conversationKey:1,createdAt:-1});messageSchema.index({groupId:1,createdAt:-1});export const Message=mongoose.model('Message',messageSchema);
