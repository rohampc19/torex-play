export class AppError extends Error{constructor(status,message,details=undefined){super(message);this.status=status;this.details=details}} export function notFound(req,res){res.status(404).json({message:'مسیر موردنظر پیدا نشد.'})} export function errorHandler(err,req,res,next){if(res.headersSent)return next(err);const status=err.status||500;const body={message:status>=500?'خطای داخلی سرور.':err.message};if(err.details)body.details=err.details;if(process.env.NODE_ENV!=='production')body.stack=err.stack;res.status(status).json(body)} export const asyncHandler=fn=>(req,res,next)=>Promise.resolve(fn(req,res,next)).catch(next);