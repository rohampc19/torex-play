import 'dotenv/config'; import {z} from 'zod'; const schema=z.object({NODE_ENV:z.enum(['development','test','production']).default('development'),PORT:z.coerce.number().int().positive().default(3000),CLIENT_ORIGIN:z.string().url().default('http://localhost:5173'),JWT_ACCESS_SECRET:z.string().min(32),JWT_REFRESH_SECRET:z.string().min(32),JWT_ACCESS_EXPIRES:z.string().default('15m'),JWT_REFRESH_EXPIRES:z.string().default('7d'),POSTGRES_URL:z.string().min(1),MONGODB_URL:z.string().min(1),COOKIE_DOMAIN:z.string().optional().default(''),UPLOAD_MAX_BYTES:z.coerce.number().int().positive().default(5242880)}); export const env=schema.parse(process.env); export const cookieOptions={httpOnly:true,sameSite:'lax',secure:env.NODE_ENV==='production',domain:env.COOKIE_DOMAIN||undefined,path:'/'};