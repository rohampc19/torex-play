import {asyncHandler,AppError} from '../middleware/error.js'; import * as service from '../services/notification.service.js'; export const list=asyncHandler(async(req,res)=>res.json({notifications:await service.listNotifications(req.user.id)})); export const read=asyncHandler(async(req,res)=>{const n=await service.markRead(req.user.id,req.params.id);if(!n)throw new AppError(404,'اعلان پیدا نشد.');res.json({notification:n})});