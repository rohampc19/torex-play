import {asyncHandler,AppError} from '../middleware/error.js'; import * as s from '../services/admin.service.js'; export const stats=asyncHandler(async(req,res)=>res.json({stats:await s.stats()})); export const pending=asyncHandler(async(req,res)=>res.json({users:await s.pendingUsers()})); export const setStatus=asyncHandler(async(req,res)=>{const u=await s.setUserStatus(req.user.id,req.params.id,req.body.status);if(!u)throw new AppError(404,'کاربر پیدا نشد.');res.json({user:u})}); export const block=asyncHandler(async(req,res)=>res.json({user:await s.blockUser(req.user.id,req.params.id)}));