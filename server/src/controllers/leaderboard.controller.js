import {asyncHandler} from '../middleware/error.js'; import {pool} from '../config/db.js'; export const list=asyncHandler(async(req,res)=>{const{rows}=await pool.query('SELECT ROW_NUMBER() OVER (ORDER BY p.score DESC,p.level DESC,u.created_at ASC) rank,u.id,u.username,p.display_name,p.avatar_url,p.score,p.level,(SELECT COUNT(*) FROM follows f WHERE f.following_id=u.id)::int followers,(SELECT COUNT(*) FROM group_members gm WHERE gm.user_id=u.id)::int group_activity FROM users u JOIN profiles p ON p.user_id=u.id WHERE u.status=\'approved\' ORDER BY p.score DESC,p.level DESC LIMIT 100');res.json({leaderboard:rows})});