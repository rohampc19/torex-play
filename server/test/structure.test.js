import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const root=path.resolve(process.cwd(),'..');const required=['client/src/App.jsx','client/src/main.jsx','client/vite.config.js','client/src/styles.css','server/src/app.js','server/src/server.js','server/src/routes/auth.routes.js','server/src/controllers/auth.controller.js','server/src/services/auth.service.js','server/src/models/message.js','server/src/middleware/auth.js','database/migrations/001_init.sql','database/migrations/002_social_features.sql','.env.example','client/public/manifest.webmanifest','client/public/sw.js'];for(const f of required)test(`exists: ${f}`,()=>assert.ok(fs.existsSync(path.join(root,f))));test('no obvious secret material',()=>{const text=fs.readFileSync(path.join(root,'.env.example'),'utf8');assert.equal(/BEGIN PRIVATE KEY|AKIA[0-9A-Z]{16}/.test(text),false)});test('migration contains core tables',()=>{const sql=fs.readFileSync(path.join(root,'database/migrations/001_init.sql'),'utf8');for(const t of ['users','sessions','follows','news','comments','groups','group_members','notifications','reports','friend_requests','blocks'])assert.match(sql,new RegExp(`create table if not exists ${t}`,'i'))});

test('social migration contains social tables and indexes',()=>{const sql=fs.readFileSync(path.join(root,'database/migrations/002_social_features.sql'),'utf8');for(const t of ['posts','post_likes','post_comments','comment_likes'])assert.match(sql,new RegExp(`create table if not exists ${t}`,'i'));for(const i of ['idx_posts_created','idx_post_comments_post_created'])assert.match(sql,new RegExp(`create index if not exists ${i}`,'i'))});
