{
  "name": "torex-play",
  "version": "2.0.0",
  "private": true,
  "description": "TOREX PLAY — gaming news, community, profiles and realtime-ready chat platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check app.js && node --check chat-v2.js && node --check admin-panel.js",
    "check:all": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
