import {useEffect,useState} from 'react';import GlassCard from '../components/GlassCard';import {api} from '../services/api';import {Loading,ErrorState,Empty} from '../components/States';import {useAuth} from '../context/AuthContext';
export default function Leaderboard(){const {user}=useAuth(),[rows,setRows]=useState(null),[myRank,setMyRank]=useState(null),[err,setErr]=useState('');useEffect(()=>{api('/leaderboard',{},false).then(r=>{setRows(r.items);setMyRank(r.myRank)}).catch(e=>setErr(e.message))},[]);if(err)return <ErrorState message={err}/>;if(!rows)return <Loading/>;return <div><p className="text-xs text-cyan-300">RANKING</p><h1 className="mb-2 text-3xl font-black">جدول امتیازات</h1>{user&&<p className="mb-6 text-sm text-slate-400">رتبه فعلی تو: <b className="text-cyan-200">{myRank??'—'}</b></p>}{rows.length===0?<Empty label="هنوز رتبه‌بندی شکل نگرفته."/>:<div className="space-y-3">{rows.map(r=><GlassCard key={r.username} className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><span className="text-cyan-300">#{r.rank}</span><b className="mr-3">{r.displayName}</b><span className="mr-2 text-sm text-slate-500">@{r.username}</span></div><div className="grid grid-cols-3 gap-4 text-left text-xs"><div><b className="block text-base">{r.score}</b><span className="text-slate-500">امتیاز</span></div><div><b className="block text-base">{r.followersCount}</b><span className="text-slate-500">دنبال‌کننده</span></div><div><b className="block text-base">{r.activity}</b><span className="text-slate-500">فعالیت · سطح {r.level}</span></div></div></GlassCard>)}</div>}</div>}
