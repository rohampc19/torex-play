import {useState} from 'react'; import {Link,useNavigate} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; import {useToast} from '../context/ToastContext'; export function Login(){const[form,setForm]=useState({identifier:'',password:''});const[busy,setBusy]=useState(false);const{login}=useAuth();const{push}=useToast();const navigate=useNavigate();async function submit(e){e.preventDefault();setBusy(true);try{await login(form);push('ورود با موفقیت انجام شد.','success');navigate('/')}catch(err){push(err.message,'error')}finally{setBusy(false)}}return <section className="mx-auto max-w-md"><div className="glass rounded-[2rem] p-6 sm:p-8"><span className="text-xs font-bold text-neon">TOREX PLAY</span><h1 className="mt-3 text-3xl font-black">خوش برگشتی 🎮</h1><p className="mt-2 text-sm text-slate-400">با نام کاربری یا ایمیل وارد شو.</p><form onSubmit={submit} className="mt-6 space-y-4"><input className="field" required value={form.identifier} onChange={e=>setForm({...form,identifier:e.target.value})} placeholder="نام کاربری یا ایمیل" autoComplete="username"/><input className="field" required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="رمز عبور" autoComplete="current-password"/><button disabled={busy} className="btn-primary w-full">{busy?'در حال ورود...':'ورود'}</button></form><p className="mt-5 text-center text-sm text-slate-400">حساب نداری؟ <Link className="text-neon" to="/register">ثبت‌نام کن</Link></p></div></section>}
