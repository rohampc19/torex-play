import {createContext,useContext,useEffect,useMemo,useState} from 'react'; import {api} from '../services/api'; const AuthContext=createContext(null); export function AuthProvider({children}){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/auth/me').then(({data})=>setUser(data.user)).catch(()=>setUser(null)).finally(()=>setLoading(false))},[]);const value=useMemo(()=>({user,loading,async login(payload){const{data}=await api.post('/auth/login',payload);setUser(data.user);return data},async register(payload){return api.post('/auth/register',payload)},async logout(){await api.post('/auth/logout');setUser(null)},refresh(){return api.post('/auth/refresh').then(r=>{setUser(r.data.user);return r.data})}}),[user,loading]);return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>} export const useAuth=()=>useContext(AuthContext);
