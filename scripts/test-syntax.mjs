import fs from 'node:fs'; import path from 'node:path'; import ts from '/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript/lib/typescript.js'; const root=process.cwd();const failures=[];function walk(dir){for(const ent of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,ent.name);if(ent.name==='node_modules'||ent.name==='dist')continue;if(ent.isDirectory())walk(p);else if(/\\.(js|mjs|jsx|ts|tsx)$/.test(ent.name)){const src=fs.readFileSync(p,'utf8');const out=ts.transpileModule(src,{compilerOptions:{jsx:ts.JsxEmit.ReactJSX,target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext},reportDiagnostics:true});if(out.diagnostics?.length)failures.push([p,out.diagnostics.map(d=>ts.flattenDiagnosticMessageText(d.messageText,' ')).join('; ')])}}}walk(path.join(root,'server'));walk(path.join(root,'client/src'));if(failures.length){console.error(JSON.stringify(failures,null,2));process.exit(1)}console.log('Syntax/JSX parse checks passed.');